# Runtime dependencies
# Install with: pip install -r requirements.txt

# Deep learning framework for training and inference
torch>=2.11.0

# BPE tokenizer (HuggingFace)
tokenizers>=0.22.2

# ONNX model format, runtime, and exporter (used by the browser demo)
onnx>=1.21.0
onnxruntime>=1.24.4
onnxscript>=0.6.2
